import WindowsTaskbarMockup from "@/components/mockups/WindowsTaskbarMockup";

export default function TaskbarPage() {
  return (
    <main className="min-h-screen bg-black">
      <WindowsTaskbarMockup autoPlay={true} />
    </main>
  );
}
